/* global AFRAME clog */
AFRAME.registerComponent('flick-cloner', {
  init: function() {
    let self = this
    const TIME_TO_STOP_MS = 100
    const MIN_PRE_FLICK_VELOCITY = 
    self.tick = function() {
      clog('flick-cloner', 'i am watching')
    }
  }
})
